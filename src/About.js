import React from 'react';
import Card from 'react-bootstrap/Card';

const About = () => {
  return (
  <main>
    <Card>
      <Card.Body>This is a Blog to practice React.</Card.Body>
    </Card>
  </main>);
};

export default About;
